#!/usr/bin/env bash
# Pre-commit hook.
#
# README "Today's commits" is maintained by .githooks/post-commit, which
# runs after each commit and creates a follow-up docs(readme) commit so
# every user commit appears with its real SHA + GitHub link.

set -euo pipefail

exit 0
